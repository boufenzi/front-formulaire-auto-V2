import { DateBuilderLimitationInputDirective } from './date-builder-limitation-input.directive';

describe('DateBuilderLimitationInputDirective', () => {
  it('should create an instance', () => {
    const directive = new DateBuilderLimitationInputDirective();
    expect(directive).toBeTruthy();
  });
});
